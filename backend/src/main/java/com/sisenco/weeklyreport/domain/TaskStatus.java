package com.sisenco.weeklyreport.domain;

/** Status of a single row in the "tasks completed" table of a report. */
public enum TaskStatus {
    NOT_STARTED,
    IN_PROGRESS,
    BLOCKED,
    COMPLETED
}
